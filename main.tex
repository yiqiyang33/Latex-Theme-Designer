\documentclass[oneside]{book}

\usepackage{amsmath, amsthm, amssymb, amsfonts}
\usepackage{thmtools}
\usepackage{lmodern}
\usepackage{ctex}

\renewcommand{\familydefault}{\sfdefault}
\usepackage{graphicx}
\usepackage{setspace}
\usepackage{geometry}
\usepackage{float}
\usepackage{hyperref}
\usepackage[utf8]{inputenc}
\usepackage[english]{babel}
\usepackage{framed}
\usepackage[dvipsnames]{xcolor}
\usepackage{environ}
\usepackage{algorithm}
\usepackage{algorithmic}
\usepackage[style=authoryear, backend=biber]{biblatex}
\usepackage{tcolorbox}
\tcbuselibrary{theorems,skins,breakable}
\addbibresource{references.bib}

\setstretch{1.2}
\geometry{
    textheight=9in,
    textwidth=7.5in,
    top=1in,
    headheight=21pt,
    headsep=25pt,
    footskip=30pt
}

% -------------------- Style Feature Toggles --------------------
% Disable chapter/section styling:
%   Change "\EnableHeadingThemetrue" to "\EnableHeadingThemefalse"
\newif\ifEnableHeadingTheme
\EnableHeadingThemetrue

% Disable table-of-contents styling:
%   Change "\EnableTOCThemetrue" to "\EnableTOCThemefalse"
\newif\ifEnableTOCTheme
\EnableTOCThemetrue

% Disable custom header/footer style:
%   Change "\EnablePageThemetrue" to "\EnablePageThemefalse"
\newif\ifEnablePageTheme
\EnablePageThemetrue

% Disable enhanced theorem/callout visuals:
%   Change "\EnableEnhancedEnvStyletrue" to "\EnableEnhancedEnvStylefalse"
\newif\ifEnableEnhancedEnvStyle
\EnableEnhancedEnvStyletrue

% Enable native amsthm theorem environments:
%   Change "\EnablePlainAmsthmTheoremfalse" to "\EnablePlainAmsthmTheoremtrue"
\newif\ifEnablePlainAmsthmTheorem
\EnablePlainAmsthmTheoremfalse

% Disable subtle block shadow effect:
%   Change "\EnableBlockShadowtrue" to "\EnableBlockShadowfalse"
\newif\ifEnableBlockShadow
\EnableBlockShadowtrue

% Optional override file generated by tools/theme_designer.py.
% Delete theme.overrides.tex if you want to fully reset to defaults above.
\InputIfFileExists{theme.overrides.tex}{}{}

% Centralized theme module.
\usepackage{theme}

% Variables
\def\notetitle{LaTeX Editing Toolkit Template Guide}
\def\noteauthor{Yiqi Yang}
\def\notedate{\today}

% The theorem system and user-defined commands
\input{theorems.tex}
\input{commands.tex}

% ------------------------------------------------------------------------------

\usepackage{subfiles}
\begin{document}
\title{\textbf{\LARGE{\notetitle}}}
\author{\noteauthor}
\date{\notedate}

\maketitle
\newpage

\tableofcontents
\newpage

\chapter{Project Overview / 项目总览}

\section{What This Repository Is / 这个仓库是什么}

This repository is a local-first LaTeX note template with a Toolkit UI.
You can tune styles, switch theorem modes, choose compile recipes, split files, and preview PDF from one place.

这个仓库是一个本地优先的 LaTeX 笔记模板，并带有 Toolkit UI。
你可以在同一套工作流中做样式调参、切换定理模式、选择编译配方、拆分文件并预览 PDF。

\section{Core Files / 核心文件职责}

\begin{itemize}
    \item \texttt{main.tex}: main entry document / 主入口文档。
    \item \texttt{theme.sty}: theme styles and color tokens / 主题样式与颜色 token 定义。
    \item \texttt{theorems.tex}: theorem + callout environments / 定理与提示框环境。
    \item \texttt{commands.tex}: custom commands / 自定义命令。
    \item \texttt{theme.overrides.tex}, \texttt{theme.colors.tex}, \texttt{theme.ui.json}: generated by Toolkit UI / 由 Toolkit UI 生成。
    \item \texttt{tools/}: Toolkit server, splitter, tests / Toolkit 服务、拆分工具与测试。
\end{itemize}

\section{Compile Paths / 编译路径}

\begin{itemize}
    \item Internal fallback pipeline / 内置回退管线：stable baseline without VSCode recipe dependency / 不依赖 VSCode recipe，作为稳定基线。
    \item VSCode recipe mode / VSCode 配方模式：reads \texttt{.vscode/settings.json} / 读取 \texttt{.vscode/settings.json} 中的 tools + recipes。
\end{itemize}

If \texttt{.vscode/settings.json} is missing on a new machine,
use Toolkit UI button \texttt{Generate VSCode settings.json}.
It only creates the file when missing and never overwrites an existing one.

如果在新机器上缺少 \texttt{.vscode/settings.json}，请在 Toolkit UI 中点击
\texttt{Generate VSCode settings.json}。
该功能只会在文件不存在时创建，不会覆盖已有配置。

\chapter{Quick Start Tutorial / 快速上手教程}

\section{Step 1: Run Toolkit UI / 第一步：启动 Toolkit UI}

Run:

\begin{verbatim}
python3 tools/latex_toolkit.py --open-browser
\end{verbatim}

Then open compile target and recipe controls in UI.

然后在 UI 中选择编译目标与 recipe，并根据需要调整 theme toggle 与颜色。

\section{Step 2: Save + Compile / 第二步：保存并编译}

Use \texttt{Save Overrides} to write
\texttt{theme.overrides.tex} and \texttt{theme.colors.tex},
then use \texttt{Compile PDF}.

点击 \texttt{Save Overrides} 会写入
\texttt{theme.overrides.tex} 和 \texttt{theme.colors.tex}，
随后点击 \texttt{Compile PDF} 即可编译。

\section{Step 3: Theorem Mode Toggle / 第三步：定理模式切换}

Toggle \texttt{Plain amsthm Theorem}:
\begin{itemize}
    \item OFF: styled tcolorbox theorem family / 关闭时：彩色盒子风格。
    \item ON: plain amsthm theorem family / 打开时：朴素 amsthm 风格。
\end{itemize}

Command signatures stay compatible in both modes.
两种模式下命令签名保持一致（例如 \texttt{\textbackslash thm\{title\}\{label\}\{body\}}）。

\section{Label Prefix Rule / 标签前缀规则}

The theorem commands keep automatic label prefix behavior with smart dedupe:

定理命令保留自动前缀并支持智能去重：
\begin{itemize}
    \item Empty label: no \texttt{\textbackslash label}.
    \item 空标签：不写 \texttt{\textbackslash label}。
    \item Raw label \texttt{foo} under \texttt{\textbackslash thm} becomes \texttt{thm:foo}.
    \item 对 \texttt{\textbackslash thm} 而言，裸标签 \texttt{foo} 会变成 \texttt{thm:foo}。
    \item Prefixed label \texttt{thm:foo} stays \texttt{thm:foo}.
    \item 已带前缀 \texttt{thm:foo} 会保持不变。
\end{itemize}

\chapter{Environment Tutorial / 环境与命令示例}

\section{Theorem Family Commands / 定理家族命令}

\defn{Definition Demo / 定义示例}{demo-defn}{
A definition block works in both theorem modes.
定义环境在两种模式下都可用。
}

\thm{Theorem Demo (raw label) / 定理示例（裸标签）}{demo-thm-raw}{
This theorem uses a raw label and will be normalized to \texttt{thm:demo-thm-raw}.
该定理使用裸标签，最终会归一化为 \texttt{thm:demo-thm-raw}。
}

\thm{Theorem Demo (prefixed label) / 定理示例（已带前缀）}{thm:demo-thm-prefixed}{
This theorem keeps the provided prefixed label.
该定理会保留你显式给出的前缀标签。
}

\lem{Lemma Demo / 引理示例}{demo-lem}{
Lemma command is compatible in both modes.
引理命令在两种模式下兼容。
}

\cor{Corollary Demo / 推论示例}{demo-cor}{
Corollary command is also compatible.
推论命令同样兼容。
}

\prop{Proposition Demo / 命题示例}{demo-prop}{
Proposition command example.
命题命令示例。
}

\clm{Claim Demo / 断言示例}{demo-clm}{
Claim command example.
断言命令示例。
}

\fact{Fact Demo / 事实示例}{demo-fact}{
Fact command example.
事实命令示例。
}

\assumption{Assumption Demo / 假设示例}{demo-assumption}{
Assumption command example.
假设命令示例。
}

We can reference Theorem~\ref{thm:demo-thm-raw} and Theorem~\ref{thm:demo-thm-prefixed}.
这里可以引用 Theorem~\ref{thm:demo-thm-raw} 与 Theorem~\ref{thm:demo-thm-prefixed}。

\section{Callout + Proof Helpers / 提示环境与证明辅助}

\begin{note}[Toolkit Tip / 工具提示]
Switch compile recipe in UI when needed, and keep internal fallback as a baseline.
需要时可在 UI 切换 recipe，同时建议保留 internal fallback 作为基线。
\end{note}

\ex{
This is an example block from \texttt{\textbackslash ex}.
这是由 \texttt{\textbackslash ex} 生成的 Example 块。
}

\rmkb{
This is a remark block from \texttt{\textbackslash rmkb}.
这是由 \texttt{\textbackslash rmkb} 生成的 Remark 块。
}

\begin{assump}
This is a plain assumption callout environment.
这是一个独立的 assumption 提示环境。
\end{assump}

\pf{
From the two theorem demonstrations above, references remain stable because
label prefixing is deterministic and deduplicated.
由上面的两个定理示例可见，标签前缀规则是确定性且可去重的，因此引用稳定。
}

\chapter{Next Steps / 后续建议}

\begin{itemize}
    \item Add your own chapters/sections directly in \texttt{main.tex}, or later re-enable split workflow based on your writing process.
    \item 可直接在 \texttt{main.tex} 中继续添加章节，或在写作规模增大后再启用 split 工作流。
    \item Keep \texttt{theme.overrides.tex} and \texttt{theme.colors.tex} when you want reproducible style snapshots.
    \item 若希望样式可复现，建议保留 \texttt{theme.overrides.tex} 与 \texttt{theme.colors.tex}。
\end{itemize}

\end{document}
